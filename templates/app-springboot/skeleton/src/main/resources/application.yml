spring:
  application:
    name: ${{ values.name }}

server:
  port: ${{ values.serverPort }}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized

logging:
  level:
    root: INFO
    com.example: DEBUG
